---
title: "Moddy Commands: Complete Reference Overview"
sidebarTitle: "Overview"
description: "A complete reference for all Moddy slash commands — covering moderation, utilities, information lookups, and server configuration."
---

Moddy uses Discord slash commands exclusively. Type `/` in any Discord message box to see the full list of available commands and their options. Every command is self-documenting: Discord shows you each parameter, whether it's required or optional, and a short description before you even submit.

## Global vs Server Commands

Moddy's commands fall into two categories depending on where you can use them.

**Global commands** are available everywhere — in your DMs with Moddy, in any server you share with Moddy, and even in servers that haven't added Moddy. These are standalone tools that don't rely on any server-specific configuration.

Examples: `/translate`, `/ping`, `/avatar`, `/roll`, `/timestamp`

**Server commands** only work inside a server where Moddy has been installed and set up. These commands interact with server-specific data like moderation cases, configuration, and module settings.

Examples: `/config`, `/cases`, `/mod case`

<CardGroup cols={2}>
  <Card title="Moderation Commands" icon="gavel" href="/commands/moderation">
    Create and manage moderation cases, apply sanctions, add notes, and review a user's history — all from slash commands.
  </Card>
  <Card title="Utility Commands" icon="wrench" href="/commands/utilities">
    Translate text, set reminders, roll dice, generate Discord timestamps, and more — available anywhere, including DMs.
  </Card>
  <Card title="Information Commands" icon="magnifying-glass" href="/commands/information">
    Look up Discord users, avatars, banners, server invites, custom emojis, and webhook details in seconds.
  </Card>
  <Card title="Configuration Commands" icon="gear" href="/commands/configuration">
    Open Moddy's server configuration panel and manage saved messages. Requires Manage Server permission.
  </Card>
</CardGroup>

<Tip>
  All Moddy commands are slash commands. There are no prefix commands like `!warn` or `!ban` — every interaction starts with `/`.
</Tip>
